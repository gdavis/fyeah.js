
all: clean setup test build

clean:
		@echo The clean task is not ready

setup:
		@echo The setup task is not ready

test:
		@echo The test task is not ready

build:
		@echo The build task is not ready

.PHONY: all clean setup test build

